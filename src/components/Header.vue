<template>
  <h4 class="title">Place Finder</h4>
</template>

<style scoped>
.title {
  font-size: 30px;
  background: -webkit-linear-gradient(var(--white), var(--black));
  background: linear-gradient(var(--white), var(--black));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
</style>
